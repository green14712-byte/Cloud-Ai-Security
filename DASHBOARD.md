# Cloud AI Security 대시보드 구현

## 1. 이번 주차 구현 방향

현재 프로젝트는 React/Vite 같은 프론트엔드 프로젝트가 아니라 Python 기반 로그 수집·분석 프로그램이다.
따라서 이번 주차 대시보드는 새 React 프로젝트를 따로 만드는 것보다 `Streamlit`으로 구현하는 방식이 가장 효율적이다.

이 방식의 장점은 다음과 같다.

- 기존 Python 코드(`risk_engine.py`, `ip_tracker.py`)를 그대로 재사용할 수 있다.
- `logs.db` 또는 MongoDB에 저장된 로그를 바로 읽어 시각화할 수 있다.
- 별도 백엔드 API 서버를 만들지 않아도 빠르게 웹 대시보드 형태를 만들 수 있다.
- “수집 → 분석 → 위험도 평가 → 자동 대응 → 시각화” 흐름을 한 화면에서 설명하기 좋다.

## 2. 실행 방법

프로젝트 폴더에서 아래 명령어를 실행한다.

```bash
pip install -r requirements.txt
streamlit run dashboard.py
```

브라우저가 자동으로 열리지 않으면 터미널에 표시되는 Local URL을 복사해서 브라우저에 붙여넣으면 된다.

## 3. 데이터 연결 방식

`dashboard.py`는 다음 순서로 데이터를 읽는다.

1. `.env`에 `MONGO_URI`가 있고 MongoDB 연결이 가능하면 MongoDB의 로그를 우선 사용한다.
2. MongoDB 연결이 안 되면 프로젝트 폴더의 `logs.db`를 사용한다.
3. SQLite에는 위험도 결과가 저장되어 있지 않을 수 있으므로, 이 경우 대시보드에서 `risk_engine.calculate_risk()`를 이용해 위험도를 즉시 계산한다.

## 4. 대시보드 화면 구성

대시보드는 5개 탭으로 구성되어 있다.

| 탭          | 역할                                                                            |
| ----------- | ------------------------------------------------------------------------------- |
| 개요        | 전체 로그 수, HIGH 이상 이벤트 수, 고유 IP 수, 평균 위험 점수, 이벤트 추이 표시 |
| 위험 이벤트 | MEDIUM 이상 이벤트를 위험 점수 순으로 정리하고 판단 근거 표시                   |
| IP 분석     | SourceIP 기준 반복 활동, 평균 위험도, HIGH/CRITICAL 발생 횟수 표시              |
| 자동 대응   | MongoDB에 저장된 ResponseActions가 있으면 대응 유형과 실행 결과 표시            |
| 로그 상세   | CloudTrail 원본 필드 중심의 상세 조회 테이블 제공                               |

## 5. 설명

이번 주차에는 기존 로그 분석 결과를 관리자가 직관적으로 확인할 수 있도록 시각화 대시보드를 구현하였다. 대시보드는 CloudTrail 이벤트를 기반으로 위험도 분포, 시간대별 이벤트 추이, 이벤트 유형별 발생 빈도, IP 반복 활동, 자동 대응 이력을 제공한다. 이를 통해 단순히 콘솔에 로그를 출력하는 수준을 넘어서, 관리자가 어떤 이벤트를 우선 확인해야 하는지 빠르게 판단할 수 있도록 하였다.

특히 위험도 평가는 기존 `risk_engine.py`의 계산 로직을 재사용하여 이벤트 기본 위험도, 시간대, 외부 IP 여부, root 계정 여부, 보안그룹/S3/IAM 관련 필드를 함께 반영한다. 따라서 대시보드는 별도의 독립 화면이 아니라, 기존 탐지·위험도 평가·자동 대응 구조와 연결된 통합 시각화 모듈로 볼 수 있다.
